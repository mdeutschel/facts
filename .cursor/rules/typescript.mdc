---
description: TypeScript-Konventionen für dieses Projekt
globs: src/**/*.{ts,tsx}
alwaysApply: false
---

# TypeScript-Konventionen

- Strict Mode, kein `any` — stattdessen `unknown` mit Type Narrowing
- Keine Semikolons, einfache Anführungszeichen
- Alle geteilten Interfaces zentral in `src/types/index.ts`
- Discriminated Unions über `type`-Feld für Variantendaten (siehe `ContentBlock`)
- Custom Hooks: Named Export, geben `{ data, loading, error }` zurück
- Type Assertions: `as Promise<Type>` auf `.json()`-Aufrufen bevorzugen statt generischer Parameter
- Modul-Level-Caching (einfache Objekte) statt React State für komponentenübergreifende Datennutzung
